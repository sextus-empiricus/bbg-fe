/* Enums copied from BE. React Create App found some issues with importing it from '@backed'.*/
export enum QuerySortBy {
   BOUGHT_AT = 'boughtAt',
   CURRENCY = 'currency',
   AMOUNT = 'amount',
   BOUGHT_FOR = 'boughtFor',
   PRICE = 'price',
}

export enum QueryOrder {
   ASC = 'asc',
   DESC = 'desc',
}
